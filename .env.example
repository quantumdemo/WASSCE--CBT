# Flask Application Secret Key
# IMPORTANT: Change this to a long, random string of characters!
# You can generate one with: python -c 'import secrets; print(secrets.token_hex(16))'
SECRET_KEY=a_very_secret_key_that_should_be_changed

# --- Email Configuration (Example for Gmail) ---
# To use Gmail, you'll need to enable "Less secure app access" or, preferably,
# create an "App Password" for your Google account.
# https://support.google.com/accounts/answer/185833
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
MAIL_DEFAULT_SENDER=("EduPrep" <your-email@gmail.com>)

# --- Database Configuration ---
# For production, you would set this to your PostgreSQL connection string.
# e.g., DATABASE_URL=postgresql://user:password@host:port/dbname
# If DATABASE_URL is not set, it defaults to the local SQLite database.
# DATABASE_URL=